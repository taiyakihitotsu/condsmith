{
  "name": "condsmith",
  "version": "0.1.0",
  "description": "TypeScript formatter for cond type.",
  "main": "dist/format.js",
  "scripts": {
    "format:smith": "npx ts-node src/format.ts",
    "biome": "biome format src/format.ts --write --verbose",
    "build": "tsc"
  },
  "dependencies": {
    "ts-morph": "^20.0.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "ts-node": "^10.9.1",
    "typescript": "^5.3.3"
  }
}
